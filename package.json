{
  "name": "group-deps",
  "version": "0.1.4",
  "description": "A tool to group dependencies in a package.json file.",
  "type": "module",
  "bin": {
    "deps": "./bin/deps.ts"
  },
  "license": "MIT",
  "peerDependencies": {
    "typescript": "^5"
  }
}
