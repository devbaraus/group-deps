{
  "name": "group-deps",
  "version": "0.1.0",
  "description": "A tool to group dependencies in a package.json file.",
  "type": "module",
  "bin": {
    "deps": "./bin/deps.ts"
  },
  "cronDependencies": {
    "sharp": "^0.32.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
